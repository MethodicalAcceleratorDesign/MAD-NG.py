local pipe = nil

local function openPipe(pipeName)
    pipe = assert(io.open(pipeName, "w"))
    pipe:setvbuf("no")
end

local function send(string)
    pipe:write(string .. "\n")
end

return{ 
    openPipe = openPipe,
    send = send,
}
